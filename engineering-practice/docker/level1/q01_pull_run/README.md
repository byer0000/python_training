# 問題

`python:3.11-slim` イメージをpullし、コンテナを対話モードで起動して
`python --version` を実行してください。

# 期待する結果

```
$ docker run -it python:3.11-slim python --version
Python 3.11.x
```

# ヒント

`docker pull`, `docker run -it`
