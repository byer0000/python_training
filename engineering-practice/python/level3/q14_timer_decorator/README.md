# 問題

デコレータ `@timer` を自作し、任意の関数の実行時間を計測して表示できるようにしてください。
q06の `withdraw()` に適用して動作確認してください。

# 期待する結果

```
withdraw 実行時間: 0.000012 秒
```
（実際の秒数は環境により異なります）

# ヒント

`import time`, `functools.wraps` を使うとデコレータ対象の関数名などが保持される。
