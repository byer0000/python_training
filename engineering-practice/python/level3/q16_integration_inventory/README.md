# 問題

これまでの内容を統合し、CSVから注文データを読み込み → クラスで在庫を管理 →
例外処理でエラーハンドリング → 結果をJSON出力 → pytestでテスト、という一連の
小さな在庫管理ツールを作成してください。余裕があれば Dockerfile でコンテナ化してください。

# 期待する結果

- `python inventory.py` を実行すると `stock_summary.json` が生成される
- `pytest` で在庫不足時に例外が送出されることがテストされている
- （発展）`docker build -t inventory-tool .` が成功する

# ヒント

q09/q10/q06/q15の実装を1つのミニプロジェクトとして組み合わせる。
