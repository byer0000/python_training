# 問題

タプルを返す関数 `min_max(nums)` を作り、リストの最小値と最大値を同時に返してください。
呼び出し側でアンパックして2つの変数に受け取ってください。

# 期待する結果

```python
min_max([3, 7, 1, 9, 4]) -> (1, 9)
```

# ヒント

`return min_val, max_val` / `a, b = min_max(...)`
