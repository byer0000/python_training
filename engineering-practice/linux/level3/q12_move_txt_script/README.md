# 問題

`practice` ディレクトリ内の `.txt` ファイルをすべて `archive/` ディレクトリに移動し、
移動したファイル数をログとして `move_log.txt` に記録するスクリプトを書いてください。

# 期待する結果

```
$ ./move_txt.sh
$ cat move_log.txt
2024-01-01 12:00:00 - 2個のファイルを移動しました
```

# ヒント

`for` ループ、`mv`、リダイレクト `>>`
