# 問題

mainブランチへのマージをトリガーに「テスト→Lint→Dockerビルド→push」までを
1つのワークフローとしてつなげ、途中のステップが失敗したら後続が実行されないことを確認してください。

# 期待する結果

わざとテストを失敗させると、それ以降のLint/ビルド/pushのステップが実行されず、
ワークフロー全体が失敗として表示される。

# ヒント

ステップは上から順に実行され、失敗すると停止する（`continue-on-error`の挙動も調べておく）。
